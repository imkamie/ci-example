name: build
on: [push]

jobs:
  build:

    name: ubuntu
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Building1
      run: cmake --build .

      - name: Building2
      run: mkdir build && cmake .. && make
	
